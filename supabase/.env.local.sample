# =====================================
# Supabase Local Environment Variables
# =====================================
# このファイルをコピーして `.env.local` を作成し、必要な値を設定してください
# cp .env.local.sample .env.local

# =====================================
# Google OAuth Configuration
# =====================================
# Google Cloud Console で OAuth 2.0 クライアントを作成し、認証情報を設定
# https://console.cloud.google.com/apis/credentials

# Google OAuth Client ID
SUPABASE_AUTH_GOOGLE_CLIENT_ID=your-google-client-id

# Google OAuth Client Secret
SUPABASE_AUTH_GOOGLE_SECRET=your-google-client-secret
